---

# Network sections from impossible_networks which should be allowed
#   / are actually being used by the current host 
# By default, there are none.
tmp:
  - ssh
services:
  "{{ tmp + (public_services if public_services is defined else empty) }}"


