#
# host_vars for "dev"
#

prod: no
ansible_ssh_host: 192.168.56.99
ansible_ssh_user: root
tmp:
  - "{{ ansible_ssh_host }}/24"
used_networks:
  "{{ tmp + (used_networks if used_networks is defined else empty) }}"


